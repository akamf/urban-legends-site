[pytest]
addopts = -sv
testpaths = tests
python_files = test_*.py
python_functions = test_*
; markers =
;     asyncio: marks tests as asyncio.
;     trio: marks tests as trio.
;     aiohttp: marks tests as aiohttp.
; filterwarnings =
;     ignore::pytest.PytestUnhandledCoroutineWarning
